packages:
  - apps/*
  - packages/*

# Packages allowed to run build scripts (native compilation/binary install).
# In pnpm >=10.26: any package with build scripts NOT listed here will cause
# ERR_PNPM_IGNORED_BUILDS. Add new native deps here when introduced.
allowBuilds:
  bcrypt: true
  esbuild: true
  sharp: true
